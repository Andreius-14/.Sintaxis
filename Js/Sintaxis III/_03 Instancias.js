class Libro {
  constructor(titulo, autor, year) {
    this.titulo = titulo;
    this.autor = autor;
    this.year = year;
  }
}

// Instancia de la clase Libro
const libro1 = new Libro("Libro 1", "Grover", "2015");
console.log(libro1);
